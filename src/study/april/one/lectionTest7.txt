3.04.2014
Как обрабатывать ошибки:
-не обрабатывать, если не критично или нам плевать
-юзать коды возврата, как у процессов (1 -- не ок, понел))0)
-флаг ошибки
-вызов метода обработки ошибки
-завершение программы

Пример, в котором мы делим на 0d, можем ставить Complex.error, возвращать null,
    вызывать изнутри divisionByZeroError(), System.exit(0);

А в моей уютной джаве есть Exception!
Долго рассказывает про всякие штуки, а тут дошел до ассертов:
throw new AssertionError(message);
assert statement : message;

Throwable -> Exception
|
v
Error

Error -- это что-то связанное с jvm, Exception -- это мой кривой код. Вот IOError -- это
проблема чтения с несуществующего хардвара
А вот есть еще RuntimeException -- нечто среднее между первыми двумя. Мы не пишем в каждом методе
throws RuntimeException (там находится nullpointer и т д), с Error кстати такая же хуита.
